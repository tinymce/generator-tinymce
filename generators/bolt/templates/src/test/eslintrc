{
  "env": {
    "browser": true,
    "amd": true
  },

  "globals": {
    "assert": true,
    "test": true,
    "asynctest": true
  },

  <% if (!internal) { %>"extends": "../../.eslintrc"<% }%>
  <% if (internal) { %>"extends": "../../../../../../.eslintrc"<% }%>
}
